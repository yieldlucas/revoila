fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
python-multipart>=0.0.9
apscheduler>=3.10
python-dotenv>=1.0
pydantic>=2.6
requests>=2.31
stripe>=9.0

# Optionnel : décommenter quand tu branches l'IA réelle
# anthropic>=0.25
# Optionnel : pour le déploiement Postgres (étape F)
# psycopg[binary]>=3.1

# Dev / tests
pytest>=8.0
